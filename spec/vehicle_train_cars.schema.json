{
  "primaryKey": [
    "train_vehicle_id",
    "car_vehicle_id"
  ],
  "missingValues": [
    "NA",
    "NaN",
    ""
  ],
  "fields": [
    {
      "name": "train_vehicle_id",
      "type": "string",
      "title": "ID referencing vehicles.vehicle_id for the entire train",
      "description": "Identifies the train vehicle record in the vehicles table.",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "car_vehicle_id",
      "type": "string",
      "title": "ID referencing vehicles.vehicle_id for the individual train car",
      "description": "Identifies the train car vehicle record in the vehicles table.",
      "constraints": {
        "required": true
      }
    },
    {
      "name": "car_order",
      "type": "integer",
      "description": "The assigned order of the car within the consist.",
      "constraints": {
        "minimum": 1
      }
    },
    {
      "name": "car_orientation",
      "type": "string",
      "description": "Indicates the orientation of the car within the consist.",
      "constraints": {
        "enum": [
          "forward",
          "backward"
        ]
      }
    }
  ],
  "foreignReferences": [
    {
      "fields": "train_vehicle_id",
      "reference": {
        "resource": "vehicles",
        "fields": "vehicle_id"
      }
    },
    {
      "fields": "car_vehicle_id",
      "reference": {
        "resource": "vehicles",
        "fields": "vehicle_id"
      }
    }
  ],
  "name": "vehicle_train_cars.schema.json",
  "description": "Assignment of vehicles/cars into consists.",
  "_table_type": "Supporting"
}
